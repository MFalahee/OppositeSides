import React from 'react'

export default function InputField(props) {
    // this will be the input field for your address/clicking find my location
    // data from this component will be passed to the backend in order to retrieve the coordinates for google maps
    return (
        <div>
            <h1>Input Field</h1>
        </div>
    )
}